# Sub2API v0.1.171 Fork 同步实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将官方 v0.1.171 的全部增量合入当前 Fork，并确保 Kiro、XorPay、Access Ban、套餐续期及 UI 定制完整保留。

**Architecture:** 使用 `v0.1.170` blob 作为三方补丁基线，将 `v0.1.170..v0.1.171` 应用到当前 HEAD。无冲突文件直接采用官方增量；冲突文件按官方变更意图与 Fork 定制语义合并，之后通过分域测试和定向静态断言验证。

**Tech Stack:** Go 1.25、Vue 3、TypeScript、pnpm、PostgreSQL、Redis、Git 三方补丁。

---

### Task 1: 建立同步基线与应用官方增量

**Files:**
- Modify: 官方 `v0.1.170..v0.1.171` 涉及的 206 个文件
- Preserve: `docs/superpowers/specs/2026-08-05-sub2api-v0.1.171-sync-design.md`

- [ ] 确认工作区除本次规格/计划外无已有改动，保存官方变更文件清单。
- [ ] 运行 `git diff --binary v0.1.170..v0.1.171 | git apply --3way --index -`，让 Git 用官方基线 blob 做真实三方合并。
- [ ] 记录未合并文件；检查无 `.env*`、生产配置或构建产物进入改动。

### Task 2: 合并后端依赖注入、认证与设置

**Files:**
- Modify: `backend/cmd/server/wire.go`
- Modify: `backend/cmd/server/wire_gen.go`
- Modify: `backend/internal/handler/{auth_*,passkey_handler.go,setting_handler.go,wire.go}`
- Modify: `backend/internal/handler/admin/setting_handler*.go`
- Modify: `backend/internal/service/{auth_service.go,setting_*.go,wire.go}`
- Modify: `backend/internal/repository/wire.go`
- Test: 官方 171 新增 captcha、settings、OAuth、Passkey 测试

- [ ] 合入腾讯/阿里云验证码服务、DTO、公开设置、路由和 Wire provider。
- [ ] 保留 `AuthService` 的 Access Ban 邮箱检查和注册邮箱格式校验。
- [ ] 保持 Kiro provider 在 Grok provider 前，并生成/校正 `wire_gen.go`。
- [ ] 运行认证、验证码、设置和 Wire 定向测试，预期 PASS。

### Task 3: 合并网关、Codex、计费与模型广场

**Files:**
- Modify: `backend/internal/pkg/openai/request.go`
- Modify: `backend/internal/service/openai_*.go`
- Modify: `backend/internal/service/gateway_*.go`
- Modify: `backend/internal/handler/{openai_*,model_plaza_handler.go,composite_platform.go}`
- Modify: `backend/internal/repository/usage_log_*.go`
- Modify: `backend/internal/securityaudit/prompt_snapshot.go`
- Test: 官方 Codex、WS、计费、模型广场、提示词审计测试及 Fork Kiro 测试

- [ ] 合入 Codex 统一身份、版本同步、过载重试与配置开关。
- [ ] 合入 composite 推理策略、模型广场图片计价和 OpenAI 重置额度缓存。
- [ ] 合入计费失败仍落日志及 Responses `output_text` 审计修复。
- [ ] 保留 Kiro credits、cache_read、429 冷却、粘性 TTL、Access Ban Gateway 路由和 Grok 行为。
- [ ] 运行相关官方测试与 Kiro 定向测试，预期 PASS。

### Task 4: 合并订阅与支付资金逻辑

**Files:**
- Modify: `backend/internal/service/{subscription_service.go,payment_refund.go}`
- Modify: `backend/internal/repository/user_subscription_repo.go`
- Modify: `backend/internal/payment/provider/stripe.go`
- Modify: `frontend/src/views/admin/orders/AdminOrdersView.vue`
- Test: 续期并发、退款、Stripe 测试及 Fork 套餐/XorPay 测试

- [ ] 将官方订阅行锁包裹在 Fork 的续期计算与备注/额度更新事务中。
- [ ] 合入余额不足 `require_force` 契约和 Stripe 幂等键。
- [ ] 保留 XorPay provider、回调、退款及前端支付宝展示。
- [ ] 运行订阅、退款、Stripe、XorPay 定向测试，预期 PASS。

### Task 5: 合并前端验证码、登录竞态与管理 UI

**Files:**
- Create: `frontend/src/components/{AliyunCaptchaWidget.vue,CaptchaChallenge.vue,TencentCaptchaGate.vue}`
- Create: `frontend/src/api/tokenRefresh.ts`
- Modify: `frontend/src/api/{auth.ts,client.ts,passkey.ts}`
- Modify: `frontend/src/stores/{app.ts,auth.ts}`
- Modify: `frontend/src/views/auth/*.vue`
- Modify: `frontend/src/views/admin/{SettingsView.vue,GroupsView.vue,AccountsView.vue}`
- Modify: `frontend/src/types/index.ts`
- Test: 官方验证码、token refresh、认证、设置测试与 Fork 品牌/支付测试

- [ ] 合入三家互斥验证码 UI 和 OAuth/Passkey 门禁。
- [ ] 合入单飞访问令牌刷新，保持现有登录与错误口径。
- [ ] 合入 Codex、composite、模型广场和退款 UI。
- [ ] 保留用户侧 Kiro→Claude、Fork 主题、Select/ConfirmDialog、倍率隐藏和 VersionBadge 行为。
- [ ] 运行前端定向 Vitest 与 typecheck，预期 PASS。

### Task 6: 版本、文档与静态红线复核

**Files:**
- Modify: `backend/cmd/server/VERSION`
- Modify: `AGENTS.md`
- Modify: `FORK_CUSTOMIZATIONS.md`
- Modify: `docs/FORK_VS_UPSTREAM.md`

- [ ] 将 Fork 版本明确设为 `0.1.171`，记录上游 tag 的 VERSION 遗漏。
- [ ] 更新同步基线、171 能力摘要、冲突高危区和验证记录。
- [ ] 静态断言迁移 157 含 Kiro/Grok、159/160 存在、Access Ban 路由存在、XorPay 类型存在、Kiro 在 Grok 前、VersionBadge 未恢复更新按钮。
- [ ] 扫描冲突标记、异常替换字符、UTF-8 BOM 和中文乱码，预期无异常。

### Task 7: 全量验证与代码审查

**Files:**
- Verify: `backend/**`
- Verify: `frontend/**`

- [ ] 在 `backend` 运行 `gofmt`（仅本次改动 Go 文件）、`go build ./...`、`go vet ./...` 和项目约定单测。
- [ ] 在 `frontend` 运行 `pnpm typecheck` 与 `pnpm vitest run`。
- [ ] 运行 `git diff --check`，核对无敏感/受保护文件和无意删除 Fork 文件。
- [ ] 对最终 diff 做代码审查，修复高优先级问题并重跑受影响验证。
- [ ] 输出修改、验证、乱码检查和残余风险，不提交、不推送。